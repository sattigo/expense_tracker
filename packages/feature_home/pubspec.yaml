name: feature_home
description: Home shell feature package with bottom navigation
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.11.1 <4.0.0"

resolution: workspace

dependencies:
  core_l10n:
    path: ../core_l10n
  flutter:
    sdk: flutter
  go_router: "14.6.2"

dev_dependencies:
  flutter_lints: "6.0.0"
  flutter_test:
    sdk: flutter
