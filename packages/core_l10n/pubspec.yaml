name: core_l10n
description: Core localization package
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.11.1 <4.0.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: "0.20.2"

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  generate: true
