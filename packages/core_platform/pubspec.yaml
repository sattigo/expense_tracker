name: core_platform
description: Platform type detection and persistence service
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.11.1 <4.0.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: "2.3.5"

dev_dependencies:
  flutter_lints: "6.0.0"
  flutter_test:
    sdk: flutter
